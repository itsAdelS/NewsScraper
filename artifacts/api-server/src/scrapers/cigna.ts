/**
 * CignaScraper — payer-specific scraper for cigna.com / evernorth.com properties.
 *
 * Currently inherits all generic behaviour.  Override postProcessContent()
 * here to add Cigna-specific extraction rules when needed in the future.
 */

import { GenericScraper } from "./generic.js";

export class CignaScraper extends GenericScraper {
  // Placeholder for Cigna-specific extraction logic.
}
