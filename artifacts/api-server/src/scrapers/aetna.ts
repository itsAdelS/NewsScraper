/**
 * AetnaScraper — payer-specific scraper for aetna.com properties.
 *
 * Currently inherits all generic behaviour.  Override postProcessContent()
 * here to add Aetna-specific extraction rules when needed in the future.
 */

import { GenericScraper } from "./generic.js";

export class AetnaScraper extends GenericScraper {
  // Placeholder for Aetna-specific extraction logic.
}
